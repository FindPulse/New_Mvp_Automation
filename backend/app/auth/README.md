# Auth Layer

Auth is intentionally not implemented in this boilerplate.

When the API is exposed beyond local/demo use, add:

- JWT or OAuth authentication
- role-based access for admin/reviewer/client
- connector credential vaulting
- request logging and rate limiting
