# Frontend

Current working UI is `streamlit_app.py`.

Run it with:

```bash
streamlit run frontend/streamlit_app.py
```

A future React approval dashboard can live in `frontend/src/`. For now, React is not implemented because the MVP needs a fast demo UI.
